targets:
  base_test:
    compression:
      type: tgz
    encryption:
      type: openssl
      password: test
      salt: false
    storages:
      local1:
        type: local
        keep: 1
        path: /backups
      local2:
        type: local
        keep: 2
        path: /backups2
    sources:
      mysql_test:
        type: mysql
        host: 127.0.0.1
        port: 3306
        database: test
        username: test
        password: test
      files_test:
        type: fs
        include:
          - /web
