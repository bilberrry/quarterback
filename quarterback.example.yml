targets:
  base_test:
    compression:
      type: tgz
    encryption:
      type: openssl
      password: test
      salt: false
    storage:
      type: local
      keep: 1
      path: /backups
    sources:
      mysql_test:
        type: mysql
        host: 127.0.0.1
        port: 3306
        database: test
        username: test
        password: test
      files_test:
        type: fs
        include:
          - /web
